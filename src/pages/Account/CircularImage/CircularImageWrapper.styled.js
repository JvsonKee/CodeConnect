import styled from 'styled-components'

const CircularImageWrapper = styled.div`
    width: 20vw;
    height: 20vw;
    border-radius: 50%;
    overflow: hidden;
`;

export default CircularImageWrapper;

